{
  "name": "next-offline-test-app",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "clean": "rm -rf .next public/manifest",
    "build": "next build",
    "start": "yarn clean && yarn build && next start",
    "test": "yarn clean && NODE_OPTIONS=--trace-deprecation jest",
    "export": "yarn build && next export"
  },
  "dependencies": {
    "next": "^10.2.3",
    "next-compose-plugins": "^2.2.1",
    "next-manifest": "^3.0.1",
    "next-offline": "^6.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "execa": "^5.0.0",
    "fs-extra": "^10.0.0",
    "jest": "^27.0.3",
    "http-server": "^0.12.3",
    "puppeteer": "^9.1.1"
  }
}
